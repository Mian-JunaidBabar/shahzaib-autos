export const metadata = {
  title: "Checkout - AM Motors",
  description: "Complete your order with AM Motors.",
};

export default function CheckoutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
