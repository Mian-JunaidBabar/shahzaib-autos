prisma.ts -> add this as true
const pool = connectionString
  ? new Pool({
      connectionString,
      ssl: {
        rejectUnauthorized: false, // Accept Supabase self-signed certificates
      },
    })
  : undefined;


